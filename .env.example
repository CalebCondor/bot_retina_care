# Puerto de la aplicación
PORT=3000

# Base de datos PostgreSQL (externa — no se levanta en Docker)
DB_HOST=187.77.15.77
DB_PORT=5430
DB_USERNAME=root
DB_PASSWORD=change_me
DB_NAME=Retina

# Ollama corre en el Mac Mini — el contenedor lo consume via host.docker.internal
# (docker-compose.yml ya lo inyecta; solo úsalo si arrancas fuera de Docker)
OLLAMA_URL=http://localhost:11434
